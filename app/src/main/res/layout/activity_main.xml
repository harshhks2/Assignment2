<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".MainActivity">

    <ImageView
        android:id="@+id/ivTakenPicture"
        android:layout_width="300dp"
        android:layout_height="300dp"
        android:layout_marginStart="30dp"
        android:layout_marginTop="30dp"
        android:layout_marginEnd="30dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        app:srcCompat="@drawable/img_pic_holder" />

    <Button
        android:id="@+id/btnTakePicture"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:text="Take Picture"
        app:cornerRadius="15dp"
        app:layout_constraintEnd_toEndOf="@+id/tvPicturePath"
        app:layout_constraintStart_toStartOf="@+id/tvPicturePath"
        app:layout_constraintTop_toBottomOf="@+id/tvPicturePath" />

    <TextView
        android:id="@+id/tvPicturePath"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginStart="30dp"
        android:layout_marginTop="15dp"
        android:layout_marginEnd="30dp"
        android:text="The path of the clicked picture will appear here:"
        android:textAlignment="center"
        android:textColor="@color/black"
        android:textSize="20sp"
        app:layout_constraintEnd_toEndOf="@+id/ivTakenPicture"
        app:layout_constraintStart_toStartOf="@+id/ivTakenPicture"
        app:layout_constraintTop_toBottomOf="@+id/ivTakenPicture" />
</androidx.constraintlayout.widget.ConstraintLayout>